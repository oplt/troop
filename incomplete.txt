# Regenerated after implementation pass — remaining gaps only.
# Source of truth for status: tasks.txt
# Do not treat older audits in chat history as current requirements.

## Still open / partial

### CI confirmation
- [~] Push and confirm Quality Action is green on `pgvector/pgvector:pg16` (CREATE EXTENSION added in initial + vector migrations).

### Workflow engine depth
- [~] Subworkflow parent auto-resume (`on_workflow_run_completed`)
- [~] Parallel policies: `fail_fast`, `N-of-M`
- [~] Full ToolExecutionService unification (OrchestrationToolbox still has direct native methods)

### Matching / routing
- [~] Semantic skill embeddings; schema IO compatibility; knowledge-source availability
- [~] Agent cost/latency/model-capability scoring depth
- [~] Single shared model router (workforce still has purpose-aware ModelCapability helper)

### Consolidation
- [ ] Drop legacy `projects` / `project_tasks` after caller migration
- [ ] Eliminate SkillPack reads / `agent.skills_json` / tables after telemetry
- [~] Generic ExternalAction/Artifact runtime (remove github_* as first-class stage ids)

### E2E
- [~] Extend PG E2E through tool execute → approval consume-once → eval of frozen SkillVersion

### Beta
- [~] MCP/A2A production hardening (lifecycle, circuit breaker, grants, test servers)

## Done in this pass (do not rework)
- pgvector CI image + CREATE EXTENSION
- auth.permitted enforced in Toolbox + ToolRegistry.execute_tool
- high-risk approvals require arguments_hash
- workflow tool approval strict 4-way bind + auto ApprovalRequest
- mandatory SkillVersion snapshot (fail closed / audited degrade)
- TaskRunStarter for workflow agent nodes (freeze + enqueue)
- ToolExecutionService for workflow tools
- condition/router/trigger/parallel/delay Celery + event resume hooks
- effective permission resolver used by matcher + ToolRegistry
- analysis_mode/fallback + richer fingerprints
- PG E2E skill snapshot immutability via production run API
