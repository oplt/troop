# Regenerated after implementation pass — remaining gaps only.
# Source of truth for status: tasks.txt

## Still open / partial

### CI confirmation
- [~] Confirm Quality Action green on `pgvector/pgvector:pg16` after push (local ruff/tests green).

### Consolidation (schema drops — deferred until CI + telemetry)
- [ ] Drop legacy `projects` / `project_tasks` tables after caller migration (router unmounted; service warns)
- [ ] Drop `skill_packs` table after telemetry shows zero production reads (`legacy_skillpack_read_telemetry`)

### Matching / MCP depth (optional next)
- [~] True embedding-based semantic skill match (token Jaccard shipped as lightweight stand-in)
- [~] MCP/A2A: session lifecycle, secret rotation, real test servers (retry + circuit breaker shipped)

## Done in this pass (do not rework)
- Subworkflow parent auto-resume (`on_workflow_run_completed`)
- Parallel join policies: `fail_fast`, `n_of_m` / `N-of-M`
- ToolExecutionService ↔ OrchestrationToolbox unification (`dispatch` / no recursion)
- Shared `model_router` used by workforce provider_resolution + run provider fallback
- Skill matcher: schema IO + knowledge availability + token semantic score
- Agent matcher: ModelCapability cost/latency/tools soft signals
- SkillPack hot-path: no skills_json writes on create/update; assignments instead; telemetry on migrate/reconcile
- External action stage id `external_action_sync` with `github_sync` alias
- Legacy ProjectsService runtime warnings + LEGACY.md
- PG E2E: approval consume-once + frozen SkillVersion eval/usage
- MCP/A2A: managed HTTP + retry/backoff + circuit breaker
