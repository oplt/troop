# Ephemeral CI values for docker compose config/smoke (not used outside GitHub Actions).
POSTGRES_USER=ci
POSTGRES_PASSWORD=ci
POSTGRES_DB=app
MINIO_ROOT_USER=ci
MINIO_ROOT_PASSWORD=ci-minio-password
