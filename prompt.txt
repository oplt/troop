You are a senior staff-level AI platform engineer, backend architect, frontend architect, database migration specialist, and production reliability engineer.

Repository:
https://github.com/oplt/troop

Work on the latest `main` branch.

Troop is being transformed from a GitHub/coding-focused multi-agent orchestrator into a generic AI workforce platform where human users and AI agents collaborate on projects and tasks across departments such as Engineering, Sales, Marketing, R&D, Operations, etc.

IMPORTANT:
The previous implementation added many database models, routers, services and frontend components, but a code audit found that several items marked complete in `tasks.txt` are only scaffolding, contain placeholders/TODOs, are disconnected from the real runtime, or have frontend/backend contract problems.

Your job is NOT to add more disconnected abstractions.

Your job is to make the existing architecture WORK END-TO-END.

==================================================
PRIMARY OBJECTIVE
==================================================

Complete this production path:

Project
→ Task
→ Task Analysis
→ Capability / Tool / Knowledge Requirements
→ Existing Skill Matching
→ Skill Gap Detection
→ SkillDraft generation when needed
→ Validation
→ SkillVersion publication
→ AgentSkillAssignment
→ Agent matching / optional agent creation
→ Runtime skill resolution
→ Tool permission / approval resolution
→ Workflow execution
→ Artifacts / evidence / review
→ Evaluation
→ Skill usage statistics
→ Skill improvement / promotion

Every stage must use the SAME canonical domain objects.

Do not create another parallel system.

==================================================
NON-NEGOTIABLE ENGINEERING RULES
==================================================

1. Inspect the existing repository thoroughly before changing code.

2. Read:
   - README.md
   - DESIGN.md
   - tasks.txt
   - current Alembic migrations
   - backend/modules/workforce/**
   - backend/modules/projects/**
   - backend/modules/orchestration/**
   - backend/modules/team/**
   - frontend/src/api/**
   - frontend/src/features/workforce/**
   - frontend/src/features/skillTemplateImport/**
   - project detail UI
   - workflow UI
   - GitHub Actions workflows

3. Do NOT trust `[x]` in tasks.txt.
Verify functionality from actual code.

4. A feature can only be marked complete when:
   - persistence works;
   - backend API works;
   - frontend works if relevant;
   - runtime actually consumes it;
   - authorization is enforced;
   - tests exist;
   - typecheck/lint/build pass;
   - migration has been validated if schema-related.

5. Remove placeholders and fake success behavior.

6. Do not return fabricated confidence percentages or success probabilities.

7. Preserve existing working GitHub/engineering workflows through compatibility adapters while genericizing the core.

8. GitHub must become an integration/use case of the generic runtime, not remain embedded in generic orchestration semantics.

9. Do not create:
   backend/modules/sales
   backend/modules/marketing
   backend/modules/hr
   backend/modules/research

Department behavior must be implemented through configuration, skills, workflows, knowledge, policies and templates.

10. Avoid unnecessary rewrites.
Adapt and migrate existing working functionality.

11. Add migrations instead of manually changing production schema.

12. All new queries must be reasonably efficient.
Avoid N+1 ORM query loops.

13. Update tasks.txt continuously, but only mark tasks `[x]` after implementation and verification.

==================================================
PHASE 0 — REPOSITORY HEALTH / CI
==================================================

This must be done FIRST.

Restore a comprehensive GitHub Actions quality workflow.

The previous workflow included:
- backend lint
- backend formatting checks
- backend tests
- frontend TypeScript typecheck
- frontend lint
- frontend Vitest
- frontend production build

Restore/improve this.

Add the newly created workforce modules and frontend features to validation.

Required CI stages:

BACKEND:
- ruff check
- ruff format --check
- pytest unit tests
- relevant integration tests
- Alembic migration validation

FRONTEND:
- pnpm install --frozen-lockfile
- pnpm exec tsc --noEmit
- pnpm lint
- pnpm test
- pnpm build

DATABASE:
- start PostgreSQL in CI
- create database
- apply migrations from previous schema to HEAD
- run smoke queries
- verify downgrade where practical

Do not continue considering implementation successful until the new CI is green.

Fix every compile/type/lint/test error discovered.

==================================================
PHASE 1 — FIX CURRENT FRONTEND BREAKAGES
==================================================

Audit all recently added workforce UI.

Known examples that must be fixed:

- wrong package import:
  `@tantml:react-query`
  must not exist.

- inconsistent skill scopes:
  frontend currently uses values such as:
    personal
    company

while backend canonical scopes include:
    task
    project
    organization
    template
    global

Choose ONE canonical terminology and use it throughout.

Preferred user-facing hierarchy:

Task
Project
Organization

Template/global may remain internal/system scopes.

Do not introduce "company" and "organization" as two meanings for the same scope.

Fix:
- SkillsPage
- SkillBuilderPage
- workforce.ts
- SkillDraft DTOs
- Skill DTOs
- SkillVersion DTOs
- Department API
- task intelligence DTOs
- agent match DTOs

Stop relying on compatibility aliases everywhere.

Define clear backend response DTOs and matching frontend TypeScript types.

Consider generating frontend types from OpenAPI if practical, but do not introduce an unnecessarily large tooling change if a clean manually shared contract is sufficient.

==================================================
PHASE 2 — REPAIR WORKFORCE DATABASE MIGRATION
==================================================

Audit:

backend/alembic/versions/e8a1c2d3f4b5_workforce_domain_foundation.py

The existing migration contains unsafe/non-portable migration logic.

Known problems include:

- SQLite-specific constructs such as:
  randomblob()
  hex()

Troop uses PostgreSQL.

- legacy SkillPack ownership appears to be assigned to the first user in the database.

This is unacceptable.

- verify every selected legacy column actually exists in the previous schema.

DO NOT modify an already-applied migration blindly if doing so would break migration history.

Determine whether:
A) this migration has not been released/applied and may safely be corrected;
or
B) a corrective migration is required.

Implement the safest strategy.

Requirements:

- PostgreSQL-compatible IDs.
- deterministic/correct ownership strategy.
- no arbitrary "first user owns everything".
- migration must be idempotent where appropriate.
- preserve legacy skill relationships.
- test with realistic legacy data.
- add migration tests.

If legacy SkillPack cannot reliably map to an owner, implement an explicit migration strategy such as organization/system ownership rather than silently assigning unrelated users.

==================================================
PHASE 3 — CANONICAL PROJECT / TASK MODEL
==================================================

There are still two project/task model families.

Legacy:
backend/modules/projects/models.py
    Project
    ProjectTask

Canonical:
    OrchestratorProject
    OrchestratorTask

The rich orchestrator models should become the canonical Project/Task system.

Audit all references to:
- Project
- ProjectTask
- projects table
- project_tasks table
- legacy project router/service/repository/schema

Create a migration/deprecation strategy.

Target:

ONE project entity.
ONE task entity.

Prefer ultimately naming them:

Project
Task

instead of exposing the implementation term "OrchestratorProject" throughout the application.

However, do not perform dangerous renames if they produce unnecessary migration risk.

Compatibility adapters are acceptable temporarily.

Requirements:

- migrate remaining data if needed.
- migrate API consumers.
- remove unused legacy router/service/repository.
- remove legacy tables when safe.
- update tests.

==================================================
PHASE 4 — FIX PROJECT / DEPARTMENT ASSOCIATION
==================================================

ProjectCreate already supports top-level:

department_id

OrchestratorProject already has:

department_id

The frontend currently may place this inside:

settings.department_id

Fix this.

Project creation/update must persist:

company_id
department_id

as first-class fields.

Settings should contain execution-specific flexible configuration, not core relational identity.

Update:
- create project
- edit project
- project DTO
- list/detail views
- department filtering

Add:

Organization
  → Departments
      → Projects

Department remains OPTIONAL.

A project may exist directly under the organization.

==================================================
PHASE 5 — MAKE AI ANALYSIS ACTUALLY USE AI
==================================================

Current workforce intelligence APIs may call services with:

provider=None

and default to:

use_llm=False

This means the "AI task analysis" is often only heuristic.

Connect workforce intelligence to the EXISTING Troop provider/model routing architecture.

Do not create a second provider selection system.

Use:
- configured provider
- routing_service
- model policy
- fallback models
- existing provider config

Task analysis should automatically resolve an appropriate configured provider/model.

Allow explicit fallback to heuristics when:
- no provider exists;
- provider fails;
- offline test mode;
- user explicitly chooses deterministic analysis.

Persist:

analyzer_version
provider
model
analysis timestamp
input fingerprint

==================================================
PHASE 6 — MAKE TASK ANALYSIS DOMAIN-GENERIC
==================================================

Current heuristics contain domain-specific keywords such as:
- greenhouse
- agriculture
- leads
- companies
- GitHub
- code

Those are acceptable only as fallback examples.

The primary analyzer must reason using:

TASK:
- title
- description
- objective
- expected output
- acceptance criteria
- labels
- task type
- risk hints

PROJECT:
- project description
- goals
- department
- project policies

KNOWLEDGE:
- available knowledge scopes
- available documents
- organization/project context

TOOLS:
- ToolDefinition catalog
- installed connectors
- available grants

SKILLS:
- existing active skill versions

OUTPUT STRICT STRUCTURED JSON:

{
  objective,
  task_category,
  required_capabilities,
  required_skills,
  required_tools,
  knowledge_requirements,
  expected_artifacts,
  acceptance_criteria,
  risk_level,
  autonomy_recommendation,
  review_requirements,
  approval_requirements
}

Do not hallucinate tools that do not exist without marking them as missing integration requirements.

Improve task analysis fingerprint so cached analysis becomes invalid when relevant context changes.

==================================================
PHASE 7 — FIX SKILL MATCHING / GAP PERSISTENCE
==================================================

Current problem:

TaskRequirement rows begin as:

coverage_status = missing

Skill matching calculates matches in memory but may not persist:

coverage_status
matched_skill_id
match_score
match_explanation

Then generate-skills can incorrectly generate skills for already-covered requirements.

Fix this architecture.

After matching, every TaskRequirement must become one of:

covered
partial
missing

Persist:

matched_skill_id
matched_skill_version_id if needed
match_score
match_explanation

Skill generation must ONLY operate on current missing requirements.

Re-analysis must appropriately invalidate outdated requirement matches.

==================================================
PHASE 8 — IMPROVE SKILL MATCHING
==================================================

Current deterministic Jaccard matcher is acceptable as a baseline but insufficient.

Implement an explainable scoring pipeline.

Hard filtering first:

- skill active?
- scope permits this task?
- task-scoped skill belongs to task?
- project-scoped skill belongs to project?
- organization skill belongs to same organization/company?
- required SkillVersion exists?

Then score:

capability coverage
tool compatibility
input schema compatibility
output schema compatibility
knowledge compatibility
scope relevance
risk compatibility
approval compatibility
historical evaluation
success rate
human acceptance
latency
cost
optional semantic similarity

Do not let an organization-level active skill receive an artificial score simply because it is active.

Return a clear scoring breakdown.

Optimize database access:
NO one-query-per-skill loops.

Use joins/batched loading.

==================================================
PHASE 9 — UNIFY SKILL SYSTEMS
==================================================

Troop currently still contains two skill systems:

OLD:
SkillPack
agent.skills_json
legacy Markdown import

NEW:
Skill
SkillVersion
SkillDraft
AgentSkillAssignment

The new system must become canonical.

Target data flow:

Markdown import
        ↓
SkillDraft
        ↓
validation
        ↓
Skill
        ↓
SkillVersion
        ↓
AgentSkillAssignment
        ↓
runtime

Modify the existing Markdown skill importer so it creates SkillDrafts rather than directly manipulating legacy SkillPack.

Preserve useful parser logic.

Do NOT maintain two independent skill builders.

Provide temporary SkillPack compatibility only where necessary.

Plan and implement SkillPack deprecation.

==================================================
PHASE 10 — IMPLEMENT REAL SKILL VALIDATION
==================================================

The current endpoint:

POST /skill-drafts/{id}/validate

must NOT remain a placeholder.

Create SkillValidationService.

Validate:

REQUIRED:
- valid name
- valid slug
- valid scope
- purpose
- when_to_use
- meaningful instructions
- capabilities

SCHEMA:
- input_schema valid JSON Schema
- output_schema valid JSON Schema

TOOLS:
- referenced tools exist
- connector exists when required
- permissions can potentially satisfy tool requirement

KNOWLEDGE:
- requirement format valid

RISK:
- risk level valid
- dangerous tool + low risk mismatch warning/error
- required approval policy for high-risk actions

DUPLICATION:
- exact slug collision
- near-duplicate capability overlap
- semantic duplicate if embeddings infrastructure is already available

QUALITY:
- empty/incoherent instructions
- overly generic "general task" skills
- conflicting constraints

Return:

validation_errors
validation_warnings
duplicate_matches
is_valid

Persist validation results on SkillDraft.

Publishing MUST fail when blocking validation errors exist.

==================================================
PHASE 11 — SKILL IMPROVEMENT LOOP
==================================================

Implement the placeholder:

POST /skills/{skill_id}/improve

Inputs may include:

feedback
evaluation_id
recent failed runs
human reviewer notes

Generate a NEW SkillDraft derived from the active SkillVersion.

Never mutate an immutable published SkillVersion.

Workflow:

SkillVersion N
→ evaluation feedback
→ improvement draft
→ human edit
→ validation
→ publish
→ SkillVersion N+1

Store provenance.

==================================================
PHASE 12 — AGENT SKILL ASSIGNMENTS BECOME RUNTIME SOURCE OF TRUTH
==================================================

THIS IS CRITICAL.

AgentSkillAssignment currently exists but runtime still primarily resolves:

AgentProfile.skills_json
→ SkillPack

Change runtime resolution.

Target:

AgentProfile
→ AgentSkillAssignment
→ Skill
→ selected SkillVersion

Version policy:

pinned
latest_active

Resolve and inject into runtime:

skill purpose
skill instructions
capabilities
required tools
knowledge requirements
constraints
input/output schemas
approval requirements
evaluation criteria

Legacy skills_json may be used only as compatibility fallback during migration.

Add migration tooling where possible to convert existing agent.skills_json references into AgentSkillAssignment rows.

==================================================
PHASE 13 — REAL AGENT MATCHING
==================================================

Improve AgentMatcher.

Input:

TaskAnalysis
TaskRequirement
matched Skills

Evaluate agents using:

assigned SkillVersion coverage
capabilities
allowed tools
ToolGrants
knowledge access
department/project membership
model capability
risk/autonomy compatibility
historical evaluation
latency
cost
availability

Return:

agent
coverage
missing capabilities
matched skills
policy incompatibilities
score breakdown
recommendation

Assignment modes:

manual
recommend
automatic

Automatic assignment must still respect policy.

==================================================
PHASE 14 — IMPLEMENT TRUE AGENT ASSEMBLY / CREATION
==================================================

Current `/assemble-agent` appears primarily to recommend combinations of existing agents.

Implement a proper distinction:

A) Recommend existing agent
B) Recommend multi-agent team
C) Create new composed agent

For "Create Agent":

1. TaskAnalysis determines requirements.
2. Select appropriate existing SkillVersions.
3. User reviews composition.
4. Create AgentProfile.
5. Create AgentSkillAssignment rows.
6. Determine allowed tools from skill requirements + grants.
7. Add project membership.
8. Optionally assign task.
9. Optionally activate agent.

Do NOT fabricate estimated success probability.

If there is insufficient evaluation history, show:

"Not enough historical data"

instead.

==================================================
PHASE 15 — IMPLEMENT RECOMMEND WORKFORCE
==================================================

Replace placeholder:

POST /tasks/{task_id}/recommend-workforce

with a real orchestration recommendation.

Combine:

TaskAnalysis
SkillMatches
SkillGaps
AgentMatches
Reviewer recommendation
Workflow recommendation
Approval requirements

Return something like:

{
  analysis,
  skill_plan: {
    existing_skills,
    missing_skills,
    drafts_to_generate
  },
  execution_plan: {
    recommended_agent,
    alternate_agents,
    recommended_team,
    new_agent_proposal
  },
  workflow,
  reviewer,
  approvals,
  warnings
}

Keep this explainable.

==================================================
PHASE 16 — TOOL REGISTRY MUST BECOME ACTUAL EXECUTION PATH
==================================================

Current situation:

NEW:
ToolDefinition
ToolProvider
ToolGrant
ActionPolicy

OLD ACTUAL EXECUTION:
OrchestrationToolbox
hard-coded if/else dispatch

Unify them.

Do NOT rewrite working GitHub/web/filesystem/tool implementations unnecessarily.

Wrap existing implementations in providers.

Target interface:

ToolProvider:
    discover_tools()
    get_schema()
    validate_permissions()
    estimate_risk()
    execute()

Implement providers initially:

NativeToolProvider
GitHubToolProvider

Architecture-ready interfaces:

MCPToolProvider
HTTPToolProvider
A2AToolProvider

But do not spend excessive time implementing MCP/A2A until native path is stable.

Real runtime tool execution MUST go through:

ToolDefinition
→ connector installation if needed
→ ToolGrant
→ ActionPolicy
→ Approval if necessary
→ ToolProvider.execute()
→ RunEvent / Audit
→ Artifact/Evidence if applicable

NativeToolProvider.validate_permissions() must NOT blindly return True.

==================================================
PHASE 17 — FIX ACTION POLICY RESOLUTION
==================================================

Audit current precedence.

Desired principle:

More specific scopes override broad defaults when valid.

Suggested hierarchy:

task
skill
agent
project
department
organization
global default

But security restrictions must not accidentally be weakened.

Implement an explicit policy resolution algorithm.

Example:

organization:
    destructive_shell = prohibited

task:
    destructive_shell = autonomous

The task MUST NOT be allowed to override a hard organization prohibition.

Support policy strength / deny precedence.

Document the algorithm.

Add tests.

==================================================
PHASE 18 — GENERICIZE EXECUTION ENGINE
==================================================

Remove coding/GitHub assumptions from generic manager-worker execution.

Current generic runtime should NOT inherently contain stages like:

github_sync

or fields like:

changed_files

Generic execution result contract:

{
  status,
  summary,
  artifacts,
  evidence,
  decisions,
  risks,
  tool_calls,
  external_actions,
  blockers,
  metrics
}

Artifacts have:

kind
title
content/ref
metadata

Engineering specialization can produce:

changed_file
commit
test_result
pull_request

Sales can produce:

lead_dataset
crm_update
email_draft
meeting_brief

Marketing can produce:

campaign_brief
content_draft
seo_report

R&D can produce:

literature_review
experiment_plan
research_report

GitHub synchronization must become an integration action/event, not a generic workflow stage.

Preserve compatibility with existing engineering runs.

==================================================
PHASE 19 — DATA-DRIVEN WORKFLOW ENGINE
==================================================

Existing models:

WorkflowDefinition
WorkflowVersion
WorkflowRun
WorkflowStepRun

must become usable.

Implement CRUD/version/publish APIs.

Supported node types:

agent
skill
tool
router
condition
parallel
approval
human_input
subworkflow
delay
trigger

WorkflowVersion contains graph:

nodes
edges
entry node

Implement runtime graph execution.

Requirements:

- deterministic traversal
- conditional edges
- parallel execution
- durable state
- retries
- resume
- approval pause/resume
- human input pause/resume
- subworkflow invocation
- run events
- step runs
- cancellation

Do NOT remove the proven existing durable execution architecture.
Adapt it.

Existing hard-coded run modes may temporarily be represented as compatibility workflow templates.

==================================================
PHASE 20 — WORKFLOW BUILDER
==================================================

Once runtime is working, improve the existing workflow frontend.

Use the existing React / MUI / @xyflow/react stack.

Allow:

Create workflow
Add node
Connect nodes
Configure node
Validate graph
Save draft
Publish version
Run
Inspect execution

Node UI should visibly distinguish:

Agent
Skill
Tool
Condition
Approval
Human Input
Parallel
Subworkflow

Do not spend time making it overly decorative.
Prioritize correctness and usability.

==================================================
PHASE 21 — EVALUATION MUST CONNECT TO REAL RUNS
==================================================

SkillEvaluation and SkillUsageStat exist.

Connect them to actual execution.

For every SkillVersion used by a run, record:

skill_id
skill_version_id
task_id
run_id
agent_id
success
human acceptance
criteria scores
latency
token usage
cost
retry count

Use SkillVersion.evaluation_criteria_json.

Implement automatic evaluation hooks where deterministic.

Allow AI judge evaluation only when configured, and mark it as AI evaluation.

Allow human acceptance/rejection.

Update SkillUsageStat.

Promotion recommendations should therefore be based on real evidence.

==================================================
PHASE 22 — AUTHORIZATION / OWNERSHIP HARDENING
==================================================

Audit all new workforce endpoints.

Do not trust foreign IDs submitted by users.

For every:

company_id
department_id
project_id
task_id
skill_id
skill_version_id
agent_id
connector_id
workflow_id

verify user/organization access.

Centralize resource-access resolution where practical.

SkillDraft creation must not permit a user to link another user's project/task/skill merely by knowing an ID.

Add authorization tests.

==================================================
PHASE 23 — GENERIC TASK STATUS UI
==================================================

Backend generic statuses are:

backlog
ready
planned
in_progress
needs_input
blocked
needs_review
needs_approval
completed
archived
cancelled

Use those as primary UI states.

Legacy statuses:

queued
approved
failed
synced_to_github

may remain readable through compatibility translation.

Do not keep them in the primary new Task workflow.

Move transition rules to a single shared backend source and expose permitted transitions through API if useful.

Avoid maintaining a different hard-coded frontend transition graph.

==================================================
PHASE 24 — CLEAN UP PROJECT DETAIL UX
==================================================

The current project detail remains too engineering-specific.

Refactor toward:

Overview

Tasks
    Board
    List
    Dependencies

Agents

Workflow

Knowledge

Files & Artifacts

Activity

Settings

GitHub-specific controls should appear under:
Integrations / Engineering-specific areas

not as universal project concepts.

Brainstorm modes should support generic categories, not only:

code review
incident triage
architecture proposal

Add generic:

research
planning
ideation
decision making
risk analysis
review
strategy

Keep engineering modes where useful.

==================================================
PHASE 25 — NAVIGATION CLEANUP
==================================================

Make canonical routes:

/projects
/projects/:projectId

Stop using `/agent-projects` as the primary public route.

Keep redirects for backwards compatibility.

Implement a meaningful:

/my-tasks

with:

Assigned to me
Assigned to agents I supervise
Needs my review
Needs my approval
Blocked / needs input

Suggested top-level information architecture:

Home

Work
    Projects
    My Tasks
    Approvals

AI Workforce
    Agents
    Skills
    Teams
    Workflows

Knowledge

Integrations

Insights
    Runs
    Quality
    Cost

Admin

Do not necessarily create nested navigation if current AppLayout does not support it elegantly.
At minimum rationalize the current menu.

==================================================
PHASE 26 — REPOSITORY CLEANUP
==================================================

Remove generated trash such as:

.understand-anything/.trash-*

from version control.

Add proper .gitignore rules.

Audit repository for:

generated files
temporary AI artifacts
obsolete code
unused compatibility files
duplicate types
dead imports
TODO placeholders

Do NOT remove useful developer knowledge files without understanding their purpose.

==================================================
PHASE 27 — TESTING
==================================================

Add strong tests.

BACKEND UNIT:

TaskAnalyzer
SkillMatcher
SkillGap
SkillValidator
SkillService
AgentMatcher
ToolPolicy
Workflow graph validation
Workflow routing
Evaluation

BACKEND INTEGRATION:

Create organization
Create department
Create project
Create task
Analyze task
Find skills
Generate missing skill
Validate
Publish
Assign skill to agent
Run task
Execute allowed tool
Require approval
Resume
Create artifact
Evaluate skill

MIGRATION:

old database
→ alembic upgrade head
→ validate data
→ application starts

FRONTEND:

SkillsPage
SkillBuilder
Departments
TaskIntelligencePanel
project creation department selector
agent matching
workflow builder
task status transitions

E2E if existing infrastructure supports it.

==================================================
PHASE 28 — REMOVE PLACEHOLDERS
==================================================

Search globally for:

TODO
placeholder
not implemented
return {}
return []
delegated

Identify anything related to the new workforce architecture.

Do not blindly remove unrelated legitimate TODOs.

But the following must not remain placeholders:

Skill validation
Skill improvement
Recommend workforce
Agent creation
Workflow execution
Tool provider execution

==================================================
PHASE 29 — tasks.txt
==================================================

Rewrite tasks.txt to reflect REAL implementation status.

Use:

[x] = fully implemented AND integrated AND tested
[~] = partial
[ ] = not done
[!] = blocked/problem

Organize:

P0 — correctness / broken behavior
P1 — core architecture integration
P2 — product improvements
P3 — ecosystem expansion

Include a "Known technical debt" section.

Do not mark something complete merely because a model/table/interface exists.

==================================================
PHASE 30 — DO NOT IMPLEMENT YET UNLESS CORE IS STABLE
==================================================

The following are lower priority:

MCP live execution
A2A external agent interoperability
skill marketplace
workflow marketplace
department marketplace
large template catalog

Prepare clean extension interfaces but DO NOT allow these to distract from fixing core execution.

==================================================
ACCEPTANCE TEST — GENERIC SALES EXAMPLE
==================================================

The final platform should support this WITHOUT writing Sales-specific backend code.

Create project:

"Belgium Greenhouse Sales Expansion"

Department:
Sales

Task:

"Identify 50 greenhouse operators in Belgium and the Netherlands that are likely candidates for our agricultural drone monitoring platform. Research each company, classify whether it operates commercial greenhouses, enrich firmographic details, verify sources, qualify each lead against our ICP and produce a structured prospect dataset."

Expected behavior:

1. Troop analyzes task.

2. Detects requirements such as:
   web research
   company discovery
   company enrichment
   classification
   source verification
   lead qualification
   structured extraction

3. Detects required tools.

4. Matches existing skills.

5. Identifies missing skills.

6. Generates ONLY missing SkillDrafts.

7. User validates and publishes them.

8. Troop matches existing agents.

9. If no suitable agent exists:
   proposes creating one composed from required skills.

10. User creates agent.

11. Agent receives versioned skills through AgentSkillAssignment.

12. Agent executes task using real skill instructions.

13. Web search/fetch goes through ToolDefinition/ToolProvider/policy.

14. Results become artifacts:
    prospect dataset
    research sources
    qualification report

15. Human reviews result.

16. Skill evaluations are recorded.

17. Skill usage stats update.

No GitHub-specific status, field, artifact or workflow stage should be required.

==================================================
ACCEPTANCE TEST — ENGINEERING EXAMPLE
==================================================

Existing Engineering functionality must continue working.

Project:
Troop Development

Task:
"Investigate GitHub issue #123, fix the defect, run relevant tests and open a pull request."

Expected:

task analysis
→ repository/code/test/GitHub skills
→ engineering agent
→ repo tools
→ code modification
→ tests
→ review
→ PR action via GitHubToolProvider
→ artifact/evidence
→ completion

GitHub PR creation is an external action/artifact.

Task should end:

completed

NOT:

synced_to_github

Legacy persisted records may still be displayed through compatibility translation.

==================================================
FINAL REQUIRED OUTPUT FROM YOU
==================================================

After implementing, provide:

1. Summary of architectural changes.

2. Files changed.

3. Database migrations created/changed.

4. Legacy code removed.

5. Compatibility adapters retained.

6. Remaining technical debt.

7. Tests added.

8. Exact commands executed.

9. Results of:
   backend lint
   backend tests
   frontend typecheck
   frontend lint
   frontend tests
   frontend build
   migration tests

10. Updated tasks.txt.

11. Any item you could not complete must remain `[ ]` or `[~]`.

==================================================
IMPORTANT IMPLEMENTATION STRATEGY
==================================================

Do NOT attempt to blindly rewrite the entire system in one pass.

Execute in this order:

1. Restore CI.
2. Make current main compile/build.
3. Fix migration.
4. Fix API/frontend contracts.
5. Fix Project/Department persistence.
6. Fix skill gap persistence.
7. Implement Skill validation.
8. Connect real provider/model routing.
9. Migrate SkillPack → Skill/SkillVersion.
10. Make AgentSkillAssignment the runtime source.
11. Improve Agent matching/creation.
12. Unify ToolProvider with actual execution.
13. Genericize runtime results.
14. Implement Workflow runtime.
15. Connect Evaluation.
16. Simplify frontend/navigation.
17. Cleanup.
18. Run full regression suite.

At the end of EACH major stage, run relevant tests.

Do not leave the repository in a partially broken state while progressing.

Most importantly:

DO NOT ADD ANOTHER PARALLEL ABSTRACTION.

Complete the data flow between the abstractions that already exist.